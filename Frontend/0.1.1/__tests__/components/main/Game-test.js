/**
 * Created by BartCallant on 19/1/16.
 */
import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';

/*
jest.dontMock('../../../scripts/components/main/Game.jsx');
const Game = require('../../../scripts/components/main/Game.jsx');


describe('Game', () =>
{
    var game, gameNode;

    beforeEach(() =>
    {
        game = TestUtils.renderIntoDocument(<Game key={null} game={null} />);
        gameNode = ReactDOM.findDOMNode(game);
    });

    it('To Be Defined', () =>
    {
        expect(gameNode).toBeDefined();
    });

    it('To Have Childnodes', () =>
    {
        expect(gameNode.childNodes.length).toBeGreaterThan(0);
    });
});
*/