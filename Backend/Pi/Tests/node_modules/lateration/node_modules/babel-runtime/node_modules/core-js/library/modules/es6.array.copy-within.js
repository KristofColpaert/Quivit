// 22.1.3.3 Array.prototype.copyWithin(target, start, end = this.length)
'use strict';
var $def = require('./$.def.js');

$def($def.P, 'Array', {copyWithin: require('./$.array-copy-within.js')});

require('./$.add-to-unscopables.js')('copyWithin');